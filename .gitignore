# Credentials — never commit tokens
.env
.env.*
*.token
*token*.json
.xurl/

# Media artifacts (upload via xurl, don't version large binaries)
*.mp4
*.mov
*.png
*.jpg
*.jpeg
*.gif

# OS / editor noise
.DS_Store
*.swp
